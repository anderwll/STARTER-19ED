generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// VARCHAR CHAR 
// INT DECIMAL 
// BOOLEAN

// model Aluno {
//   // nome   tipo  
//   id String @id @default(uuid()) @db.Uuid // @id => PRIMARY KEY

//   nomeDaMinhaColuna  String @map("nome_da_minha_coluna") @db.Text // string => TEXT nome_da_minha_coluna
//   nomeDaMinhaColuna1 String @db.VarChar(150) // string => VARCHAR(150)

//   @@map("alunos")
// }

model Aluno {
  id       String @id @default(uuid()) @db.Uuid
  nome     String @db.VarChar(60)
  email    String @unique @db.VarChar(50)
  idade    Int? // opcional => NULL
  password String

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("alunos")
}
